export enum SelectedPage {
  Home = "home",
  OurClasses = "ourclasses",
  Benefit = "benefit",
  ContactUs = "contactus",
}

export interface ClassType {
  image: string
  title: string
  description: string
}